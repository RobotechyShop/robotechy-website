# Robotechy Order Service Configuration

# Merchant's Nostr secret key (nsec format)
# Get this from your Nostr wallet/client (keep it secret!)
MERCHANT_NSEC=nsec1...

# Lightning Address for receiving payments
# This is where invoices will be generated from (e.g., yourname@getalby.com)
LIGHTNING_ADDRESS=robotechy@getalby.com

# Fallback relays if NIP-65 relay list lookup fails
# Comma-separated list of relay WebSocket URLs
FALLBACK_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.primal.net
